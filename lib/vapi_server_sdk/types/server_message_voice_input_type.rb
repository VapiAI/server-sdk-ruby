# frozen_string_literal: true

module Vapi
  # This is the type of the message. "voice-input" is sent when a generation is
  #  requested from voice provider.
  class ServerMessageVoiceInputType
    VOICE_INPUT = "voice-input"
  end
end
