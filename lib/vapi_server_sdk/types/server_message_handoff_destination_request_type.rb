# frozen_string_literal: true

module Vapi
  # This is the type of the message. "handoff-destination-request" is sent when the
  #  model is requesting handoff but destination is unknown.
  class ServerMessageHandoffDestinationRequestType
    HANDOFF_DESTINATION_REQUEST = "handoff-destination-request"
  end
end
