# frozen_string_literal: true

module Vapi
  # This is the type of the message. "transfer-destination-request" is sent when the
  #  model is requesting transfer but destination is unknown.
  class ServerMessageTransferDestinationRequestType
    TRANSFER_DESTINATION_REQUEST = "transfer-destination-request"
  end
end
