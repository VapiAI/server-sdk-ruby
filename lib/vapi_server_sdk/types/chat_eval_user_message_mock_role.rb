# frozen_string_literal: true

module Vapi
  # This is the role of the message author.
  #  For a mock user message, the role is always 'user'
  #  @default 'user'
  class ChatEvalUserMessageMockRole
    USER = "user"
  end
end
