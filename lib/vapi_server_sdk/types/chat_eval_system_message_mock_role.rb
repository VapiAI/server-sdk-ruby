# frozen_string_literal: true

module Vapi
  # This is the role of the message author.
  #  For a mock system message, the role is always 'system'
  #  @default 'system'
  class ChatEvalSystemMessageMockRole
    SYSTEM = "system"
  end
end
